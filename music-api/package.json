{
  "name": "nie-music-cloudflare-api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply nie-music --local",
    "db:migrate:remote": "wrangler d1 migrations apply nie-music --remote"
  },
  "devDependencies": {
    "wrangler": "^4.26.0"
  }
}
