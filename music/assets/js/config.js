window.NIE_MUSIC_CONFIG = Object.freeze({
  // 部署 Cloudflare Worker 后改成你的地址，例如：
  // https://nie-music-api.your-account.workers.dev
  WORKER_BASE_URL: "",
  APP_NAME: "Nie Music",
  DEFAULT_SEARCH_LIMIT: 12
});
